import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-admin-selection',
  imports: [RouterLink],
  templateUrl: './admin-selection.html',
  styleUrl: './admin-selection.css',
})
export class AdminSelection {}
